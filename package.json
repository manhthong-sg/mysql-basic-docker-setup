{
  "name": "manhthongbui",
  "version": "1.0.0",
  "description": "manhthongbui",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "migrate": "npx sequelize-cli db:migrate",
    "migrate:undo": "cross-env NODE_ENV=development npx sequelize-cli db:migrate:undo",
    "migrate:undo:all": "cross-env NODE_ENV=development npx sequelize-cli db:migrate:undo:all",
    "migrate:prod": "cross-env NODE_ENV=prod npx sequelize-cli db:migrate"
  },
  "author": "manhthongbui",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.3.1",
    "mysql2": "^3.6.0",
    "sequelize": "^6.32.1",
    "sequelize-cli": "^6.6.1"
  }
}
